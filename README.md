# mochajs-multithread-workers
Using experimental nodejs workers module to parallel mocha runs


run tests: 
npm start

this will run 30 worker threads with simple selenium test


npm run report - to generate and open allure html
